{
    IBClasses = (
        {
            ACTIONS = {dummyHide = id; dummyShow = id; toggleMainHotKey = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {jcMenu = NSMenu; mainRecorder = ShortcutRecorder; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}